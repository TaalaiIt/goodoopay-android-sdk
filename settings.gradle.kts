pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "goodoo-pay-android-sdk"
include(":sdk")
include(":example:app")
